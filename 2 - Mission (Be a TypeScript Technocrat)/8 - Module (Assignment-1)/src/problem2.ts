
const getLength  = (input: string | number[]): number=>{
    if(typeof input === 'string'){
        return input.length
    }
    else {
        return input.length
    }
}

const arr: number[] = [2,34,556]
console.log(arr.length);